import { SafeAreaView } from 'react-native';

import Login from './modules/login/pages/Login';

const App = () => {
  return (
    <SafeAreaView>
      <Login />
    </SafeAreaView>
  );
};

export default App;
